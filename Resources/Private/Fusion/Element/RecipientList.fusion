prototype(NeosRulez.DirectMail.FormFinisher:RecipientList.Definition) < prototype(Neos.Form.Builder:FormElement.Definition) {
    formElementType = 'NeosRulez.DirectMail.FormFinisher:RecipientList'
    properties {
        placeholder = ${null}
        recipientLists = NeosRulez.DirectMail:Class.RecipientList
        userSelect = ${null}
        recipientList = ${null}
        inBackend = ${site.context.inBackend}
    }
}
